import { Article } from "./types";
declare type InputArticle = Omit<Article, "id" | "url">;
/**
 * Create an article in a publication.
 *
 * @param publicationId The publication's id.
 * @param article The article to be created.
 */
declare const createPublicationArticle: (publicationId: string, article: InputArticle) => Promise<Article>;
export default createPublicationArticle;
